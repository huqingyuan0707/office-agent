# ADR-0003：智能办公 Agent 拆分为独立开源项目（内核提取式）的决策

- 日期：2026-09-19
- 状态：接受（M0 骨架提取未启动）
- 决策人：项目负责人（集成 vs 拆分重议确认）
- 关联文档：`多模态智能电商客服系统需求文档-FRDv2.md` §11.2 约束 #8 / §11.6 P2 预留位、`多Agent系统架构与落地需求.md` §2.3、`office-agent仓库骨架与内核提取方案.md`（配套设计文档）
- 取代：2026-09-19 早前「集成进本项目（modules/agent/office/ 子包）」的会话内决策（开发文档草稿未落盘，主体迁移新仓库 docs/）

## 1. 背景与约束
- 现状：Agent Runtime 内核已在 `backend/app/modules/agent/` 落地并稳定（全量 pytest 296 passed）；原方案为仓库内集成 `office/` 子包承载办公域工具。
- 约束变更（触发本次重议）：
  1. 办公 Agent 需集成**非电商领域**办公场景（文档/日程/待办/公告/通用 BI），且要**开源**——电商表语义（finance_bills/purchase_orders 等）出现在开源主包会成为采用障碍；
  2. 开源项目生命周期需与本项目发版/种子数据/FRD 验收解耦。
- 硬约束：提取是**复制不是搬走**——本项目内核已测资产零回归；两仓库内核并行期以本项目为准；领域无关性为开源主包第一验收项（grep 无电商词元）。

## 2. 候选方案
| 方案 | 优点 | 缺点 | 成本 |
|---|---|---|---|
| A（原决策）：仓库内集成 `modules/agent/office/` | 零提取成本、同进程复用底座 | 开源需整仓或强裁剪、电商语义耦合、生命周期绑死 | 低 |
| B：独立项目从零重写 | 最干净、无历史包袱 | 权限/审批/审计/调度四件套重写，口径漂移风险 | 高 |
| C（本决策）：内核提取式拆分 | 四件套（contracts/registry/policy/executor/runtime）均无电商语义直接复用；两仓库互为生态 | 双仓维护、内核同步成本 | 中 |

## 3. 决策
- 选择：C——新建开源仓库 `office-agent`（Apache-2.0），从本项目复制提取内核；本项目后续定位「电商参考实现 + 第一个插件包消费者」（tools-ecommerce M2 以 HTTP 桥接、M3 升标准 MCP 注册回 office-agent，两仓库串联与数据交互设计见配套方案 §7）。
- 理由（对照约束逐条）：
  1. 领域无关 + 开源：唯一同时满足「主包无电商语义」与「不重写四件套」的方案；
  2. 底座复用：注册中心（ToolSpec+Scope+幂等+熔断）、审批闸门（恒送审）、tasks 长任务（checkpoint/SSE）、RBAC/租户隔离、trace 审计全部随内核平迁——办公 Agent 最难的四件事（权限/审批/审计/数据口径）本项目已钉死口径，重做即重复付费（`多Agent系统架构与落地需求.md` §2.3 防重复实现）；
  3. 生命周期解耦：开源仓库独立 issue/PR/发版节奏；FRD §11.6 本就把 MCP wire 列为 P2 平迁位，本决策是其自然兑现；
  4. 红线输出：恒送审、幂等键、数值不可编造（verify_numbers 后置校验）、降级不 500，是开源 Agent 框架稀缺的审批闸门层，构成差异化卖点。

## 4. 后果
- 正面：内核四件套成为开源差异化资产；本项目获得官方插件示例；社区生态独立演进；FR-9 成本归因在对接后可分域观测。
- 负面/风险 + 缓解：
  - 内核双份漂移——缓解：并行期冻结开源侧内核功能新增（只做领域无关化改造），内核改动先落本项目再移植；稳定后开源侧独立发版。
  - 提取引入兼容问题——缓解：逐文件映射表核对（配套方案 §3），每文件 py_compile + 原测试移植，领域无关性 grep 门禁入新仓库 CI。
- 回滚方案：内核是复制不是搬走，office-agent 停滞不影响本项目任何行为；MCP 化对接按 FRD §11.6 P2 节奏可中止，无 DDL 与存量数据迁移。
- 可观测验证：新仓库 `/governance/status` 等价巡检（工具注册数/定时任务最近成功时间）；对接后电商侧 token 成本归因可见 office 域。

## 5. 落地清单
- [x] `ADR-0003-办公Agent拆分独立开源项目.md`（本文件）+ `office-agent仓库骨架与内核提取方案.md` 落盘
- [x] `执行步骤.md` 升 v0.3.33 记录本决策
- [x] FRDv2 §11.2 约束 #8 行 + §11.6 P2 预留位行补「决策见 ADR-0003」
- [ ] 新仓库骨架 + 内核提取（映射表见配套方案 §3）+ Apache-2.0 LICENSE + 中英 README（M0）
- [ ] 领域无关化验收：主包 grep 无 finance/purchase/risk/sku 等电商词元；docker compose 五分钟 demo（M1）
- [ ] tools-ecommerce v1 HTTP 桥接（M2，独立 PR：6 连接器 + 查询 service 包 ToolSpec 薄壳，调电商 REST API + JWT + 幂等键透传）→ M3 升标准 MCP Server
