# ADR 规范与模板（选型变更先写 ADR，不直接改代码）

> 对齐：`AGENTS.md` §5。当要换模型/向量库/队列/协议/鉴权方案时，先提 ADR，评审通过再编码。

## 规范

- 编号递增：`docs/adr/0001-*.md`（本仓库为扁平结构，暂存根目录 `ADR-0001-*.md`亦可，首选本文件同级 `ADR-xxxx.md`）。
- 状态：`提议 → 接受/拒绝 → 已落地/已废弃`，必须写日期与决策人。
- 一事一 ADR，写清背景、约束、候选、决策、后果（含回滚）。
- 关联改动：PR 描述必须链到 ADR 编号；落地后更新对应规范文档（API/数据/RAG/页面）。

## 模板（复制后填）

```md
# ADR-000X：<标题，如 向量库由 Chroma 切换 pgvector 的决策>

- 日期：2026-XX-XX
- 状态：提议
- 决策人：___
- 关联文档：数据模型与存储设计.md §X / RAG规范 §X

## 1. 背景与约束
- 现状与问题（量级/QPS/成本/合规）：
- 约束（必须兼容适配层/租户隔离/回滚时间）：

## 2. 候选方案
| 方案 | 优点 | 缺点 | 成本 |
|---|---|---|---|
| A | | | |
| B（现状） | | | |

## 3. 决策
- 选择：___
- 理由（对照约束逐条）：

## 4. 后果
- 正面：
- 负面/风险 + 缓解：
- 回滚方案（向前兼容步骤，先加后删）：
- 可观测验证（status()/指标/压测阈值）：

## 5. 落地清单
- [ ] 代码适配层改动（业务不直连具体库）
- [ ] Settings + _HOT_FIELDS 更新
- [ ] 迁移/Alembic + 灰度 + 回滚演练
- [ ] 规范文档同步 + openapi/status 自查
```
