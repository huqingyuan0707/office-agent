# 项目编码规范｜AI编码约束文档
项目：智能办公Agent系统
技术栈：Python 3.11, FastAPI, LangGraph, Ollama, Pydantic；Milvus 与 Redis 为分阶段引入的可选组件（未配置即降级现行为，见 docs/ADR-0005）

## 一、基础约束（AI必须遵守）
1. 单次任务**优先最小改动，尽量控制在1个文件；如需多文件修改，提前列出全部改动清单，人工确认后再执行**。禁止无告知自动全局重构整个项目。
2. 不允许私自新增第三方pip依赖。如需import新包，先说明用途、版本，等待人工审批，不允许引入不存在的包。
3. 不删除原有业务代码，如需删除旧逻辑，先注释+日志标记，交由人工确认。

## 二、代码风格与命名规范
1. python：变量/函数使用snake_case；类名使用PascalCase；常量大写SNAKE_CASE。
2. 所有函数必须写文档字符串docstring，说明入参、返回值、异常场景。
3. 禁止裸except（`except:`），捕获异常必须指定异常类型，增加error日志。
4. 禁止硬编码密钥、数据库地址、API Token，全部读取环境变量`.env`。
5. 导入顺序：标准库 → 第三方库 → 项目内部模块，import分组换行。

## 三、异常、日志、返回规范
1. 所有外部调用（Ollama接口、Milvus向量库、Redis缓存）必须增加超时、重试、异常捕获。
2. 使用项目统一日志logger，区分INFO/WARNING/ERROR级别，日志带上request_id。
3. API返回统一封装响应体，固定成功/错误结构，错误返回可读message + error_code。
4. 参数校验全部使用Pydantic模型，禁止在业务代码手写大量if判断校验。

## 四、安全强制规则（重点，不可绕过）
1. 用户输入必须做过滤、校验，防止prompt注入。
2. 权限、数据查询逻辑，**AI写完之后必须人工逐行review**。
3. 禁止直接把用户原始输入拼接进SQL、大模型prompt，使用参数化/模板渲染。
4. 敏感数据不能打印到日志（密钥、用户隐私）。

## 五、单元测试要求
1. 新增业务函数，附带对应的pytest单元测试。
2. 边界case、异常场景必须覆盖。

## 六、CI校验准入规则
代码提交前必须通过全部检查，任意一项不通过不可合并：
- ruff lint静态检查
- mypy类型检查
- pytest单元测试全部通过
- 项目可正常启动构建

## 七、Git与提交规范
1. 功能拆分小PR，单次提交只解决一个需求点。
2. commit描述简洁，说明本次改动目的。
3. 保留稳定版本，支持快速回滚。

## 八、大模型Agent专属额外规则
1. LangGraph节点拆分清晰，单个节点职责单一，不要把多个逻辑揉在同一个节点。
2. Milvus向量检索部分，检索参数、召回阈值写在配置，不要写死代码。
3. Redis用于会话缓存、临时状态存储，key统一命名规范，设置合理过期时间。
4. Prompt统一放到prompt模板文件，不要直接写在业务代码里。
5. 输出结果增加格式校验，校验大模型返回json合法性。

## 九、输出约定
完成代码后：
1. 输出改动文件路径 + 修改说明
2. 标注潜在风险点
3. 给出单元测试
